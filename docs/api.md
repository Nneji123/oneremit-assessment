# API Foundation

The backend currently exposes `GET /health/`, returning a small JSON readiness
response. OpenAPI scaffolding is available at `/api/schema/` and `/api/docs/`.

Transfer endpoints and webhook behavior are intentionally not implemented in
Task 1. Their contracts will be documented here alongside the implementation.
